package com.example.project.utils;

public class TestClass {
    public static void main(String[] args) {
        /*JsonObject job;
        String emp_id = "\"January\"";
        System.out.println("Before "+ emp_id);
        emp_id= emp_id.substring(1, emp_id.length() - 1);
        System.out.println("After "+emp_id);
        try {

            Integer.getInteger(emp_id);
            System.out.println(Integer.parseInt(emp_id));
        }
        catch (Exception e) {
            System.out.println(e);
        }
        Integer id = 2;
        String payment_date = "\"January\"";

        String payDate = payment_date.substring(1, payment_date.length() - 1);
        Employee_Salary result;
        try {
            EmployeeSalaryDaoImpl employeeSalaryDao = new EmployeeSalaryDaoImpl();
            result = employeeSalaryDao.retriveSalary(id, payDate);
        } catch (Exception e) {
            System.out.println(e);

        }*/
    }
}
